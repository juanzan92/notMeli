import React, { Component } from 'react';

class LogInPage extends Component {
    render() {
        return (
            <Grid
                >
            </Grid>

        );
    }
}
export default (LogInPage);